<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>글쓰기</title>
</head>
<body>
    <h1><%= post ? '글 수정하기' : '새 글 작성' %></h1>

    <form action="<%= post ? '/edit/' + post.id : '/write' %>" method="POST">
        <p>제목: <input type="text" name="title" value="<%= post ? post.title : '' %>" required></p>
        <p>작성자: <input type="text" name="author" value="<%= post ? post.author : '' %>" required></p>
        <p>내용:</p>
        <textarea name="content" rows="10" cols="50" required><%= post ? post.content : '' %></textarea>
        <br><br>
        <button type="submit"><%= post ? '수정완료' : '등록하기' %></button>
        <a href="/">취소</a>
    </form>
</body>
</html>